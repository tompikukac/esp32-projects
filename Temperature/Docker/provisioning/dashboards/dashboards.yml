apiVersion: 1

providers:
  - name: 'Lakas'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # This is the path INSIDE the container
      path: /etc/grafana/provisioning/dashboards